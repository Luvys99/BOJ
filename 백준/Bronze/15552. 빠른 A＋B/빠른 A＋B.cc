#include <bits/stdc++.h>
using namespace std;

int main()
{
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);

	int Tcase;
	cin >> Tcase;

	int A, B;
	for (int i = 0; i < Tcase; i++)
	{
		cin >> A >> B;
		cout << A + B << "\n";
	}
}